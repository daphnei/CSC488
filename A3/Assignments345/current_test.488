begin
  integer function compute(integer var) begin
    put skip, var, skip
    if var = 50 then
      return (100)
    else
      return (compute(var + 1))
    end
  end
  integer x
  x <= compute(1)
  put x
end
